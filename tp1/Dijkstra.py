class PathFinder:
	def __init__(self, graph, source_node):
		self.graph = graph						# Graph  object
		self.departure_node = source_node		# Vertex object
	
	def get_shortest_path(departure, destination):
		pass

	def dijkstra_algorithm(graph, starting_node):
		amount_of_nodes = len(self.graph.list_vertex)
		shortest_distances_from_source_node = ['inf' in range(amount_of_nodes)]
		unvisited_graphs = []
		visited_graphs	 = []


